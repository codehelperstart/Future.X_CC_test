# 开发环境Dockerfile
FROM node:18-alpine

# 设置工作目录
WORKDIR /app

# 安装nodemon和必要的全局包
RUN npm install -g nodemon

# 设置用户和权限
RUN addgroup -g 1001 -S nodejs
RUN adduser -S nodejs -u 1001

# 创建必要的目录
RUN mkdir -p /app/server/uploads /app/server/logs
RUN chown -R nodejs:nodejs /app

# 切换到nodejs用户
USER nodejs

# 复制package.json文件
COPY --chown=nodejs:nodejs ./server/package*.json /app/server/

# 切换到server目录并安装依赖
WORKDIR /app/server
RUN npm install

# 暴露端口
EXPOSE 5000 9229

# 使用nodemon启动应用（支持热重载和调试）
CMD ["nodemon", "--inspect=0.0.0.0:9229", "server.js"]